using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ShareTester : MonoBehaviour
{
    public float[] beta = { 0, 0, 0, 0 };
    public float[] pose;



    // Start is called before the first frame update
    void Start()
    {
        pose = new float[24 * 3];
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
